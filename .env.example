# Copy to .env and fill in. NEVER commit .env.
# Only the PUBLIC anon key lives here — real security is Supabase RLS.
VITE_SUPABASE_URL=https://ztzgnorjnffpgytnwnvy.supabase.co
VITE_SUPABASE_ANON_KEY=

# Bold identity key (PUBLIC by design — the secret key only exists in Supabase Edge Function secrets)
VITE_BOLD_IDENTITY_KEY=
